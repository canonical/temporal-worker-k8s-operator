name: Tests

on:
  pull_request:

jobs:
  unit-tests:
    permissions:
      contents: read
      issues: write
      pull-requests: write
    uses: canonical/operator-workflows/.github/workflows/test.yaml@main
    secrets: inherit
